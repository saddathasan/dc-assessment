// Navigation Section (MS-2): floating bar + mega-menu + hamburger overlay.
// TDD stub — behavior lands per the failing contract in Navigation.test.tsx.

/** The page's Navigation Section: bar, mega-menu, and mobile overlay (D-025 slice). */
export function Navigation() {
  return <header />
}
